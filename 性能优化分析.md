# CATUI 性能问题分析

> 分析日期：2026-08-01
> 分析范围：`Source/` 下 33 个 Harmony 补丁、3 个自定义视图（`Views/`）、UIAtlases 图集打包机制、NGUI 反编译源码热路径
> 核心结论：**图片数量与图集本身无性能问题**（游戏会把 `UIAtlases/` 下 PNG 打包成单张图集，加载一次并缓存）；主要开销集中在**每帧 UI 刷新链**与**全局无差别 Hook**。

---

## 概览

| 等级 | 数量 | 说明 |
|------|------|------|
| P0 | 1 | 全局每帧开销，影响所有玩家、所有窗口，收益最大 |
| P1 | 2 | HUD 每帧刷新链，常驻 HUD 开销 |
| P2 | 3 | 物品栏模板 / 每格刷新开销 |
| P3 | 4 | 低影响 / 设计隐患 |

---

## P0 - 全局每帧开销（优先修复）

### 1. GlowEffectPatch 字典污染 + UIDrawCall.UpdateMaterials 每帧全表遍历

**位置**：`Source/Glow/GlowEffectPatch.cs:217-227`（ParseInitialAttributeValuePrefix）、`GlowEffectPatch.cs:348-370`（UpdateMaterialsPostfix）

**问题描述**

glow 功能在当前 XML 中**完全未被使用**（无 `CATUI_glowsprite`、无 `glow*` 属性），但补丁对全局产生恒定开销：

1. `ParseInitialAttributeValuePrefix` 对 UI 中**每一个** `XUiView_WidgetBased` 视图（几乎所有 sprite/label/panel/button）在**解析任意属性**时都会执行 `_glowConfigs[view] = new GlowConfig()`（代码在 switch 判断属性名之前就插入配置）。结果 `_glowConfigs` 膨胀到**全 UI 数千条**。
2. `UpdateMaterialsPostfix` 挂在 `UIDrawCall.UpdateMaterials` 上。NGUI 中该方法经由 `OnWillRenderObject` **每帧对每个可见 drawcall 调用**（`NGUI-Decompiled/UIDrawCall.cs:622-624`），postfix 每次遍历 `_glowConfigs.Keys` 全部条目。

**影响量级**

每帧 ≈ (可见 drawcall 数 ~几十) × (_glowConfigs 条目数 ~数千) = **每帧数十万次字典枚举**。恒定、全局、与玩家操作无关的 CPU 损耗，还会拖累 `updateData` 路径（每视图一次 `ContainsKey`）。

**修复建议**（按收益排序，任选其一，推荐 A+B）：

- **A. 只在 `glow*` 属性时才注册配置**（根治字典污染）
- **B. `UpdateMaterials` 只遍历 `Enabled` 的视图 / 按 UIDrawCall 建立反向索引**
- **C. glow 未启用时完全不 Hook `UpdateMaterials`**

```csharp
// A. 修复 ParseInitialAttributeValuePrefix —— 仅在属性是 glow* 时才创建配置
[HarmonyPrefix]
[HarmonyPatch(typeof(XUiView), "ParseInitialAttributeValue")]
public static bool ParseInitialAttributeValuePrefix(XUiView __instance, string _attribute, string _value)
{
    if (__instance is not XUiView_WidgetBased view) return true;
    // 只有 glow 相关属性才进入处理，避免为所有视图创建配置
    if (!_attribute.StartsWith("glow", StringComparison.OrdinalIgnoreCase)) return true;

    var config = GetConfig(view) ?? (_glowConfigs[view] = new GlowConfig());
    switch (_attribute)
    {
        case "glowenable":
            if (!_value.Contains("{")) config.Enabled = StringParsers.ParseBool(_value);
            return false;
        case "glowintensity":
            if (!_value.Contains("{") && float.TryParse(_value, out float i)) config.Intensity = Mathf.Clamp(i, 0f, 10f);
            return false;
        // ... 其余 glow* 属性同理
        default:
            return true;
    }
}
```

```csharp
// B. 修复 UpdateMaterialsPostfix —— 先跳过无启用配置的快速路径，并限定遍历范围
[HarmonyPostfix]
[HarmonyPatch(typeof(UIDrawCall), "UpdateMaterials")]
public static void UpdateMaterialsPostfix(UIDrawCall __instance)
{
    if (__instance == null || __instance.dynamicMaterial == null) return;
    if (_glowConfigs.Count == 0) return;                 // 无任何 glow 视图时零开销

    foreach (var kvp in _glowConfigs)                    // 用 KeyValuePair 遍历避免重复取值
    {
        var view = kvp.Key;
        var config = kvp.Value;
        if (config == null || !config.Enabled) continue; // 只处理启用的视图
        if (view.widget != null && view.widget.drawCall == __instance)
        {
            var dynMat = __instance.dynamicMaterial;
            if (dynMat.shader != _glowShader) { /* ... 恢复 shader ... */ }
            break;
        }
    }
}
```

> 更彻底的方案：为每个启用 glow 的 drawCall 维护 `Dictionary<UIDrawCall, XUiView_WidgetBased>` 反向索引，`UpdateMaterials` 内 O(1) 查表，完全消除遍历。

---

## P1 - HUD 每帧刷新链

### 2. HUDStatBar 无条件 IsDirty + 每帧 6 次 EffectManager.GetValue

**位置**：`Source/XUiC_HUDStatBarPatch.cs:56-71`（EnsureCacheFrame）、`73-131`（效果值缓存）、`158-797`（各 binding）

**问题描述**

- 大量 CATUI binding（`CATUI_playerPing`/`PingColor`/`PingVisible`、`CATUI_coretemp`、`CATUI_playerMoveSpeed`、`CATUI_playerActiveItem*` 等）在 `GetBindingValueInternal` 里**无条件** `__instance.IsDirty = true`，导致 `XUiC_HUDStatBar.Update` 每帧进入 `RefreshBindings()`（`Assembly-CSharp-Decompiled/XUiC_HUDStatBar.cs:202-211`）。
- 每次刷新都会触发 6 个 `EffectManager.GetValue` 全量被动效果计算：Mobility / PhysicalDamageResist / RunSpeed / BarteringBuying / BarteringSelling / EntityPenetrationCount。虽然 `EnsureCacheFrame` 做了帧缓存（同帧只算一次），但**每帧至少各算一次**，每次 `GetValue` 都遍历玩家全部装备、技能、buff、挑战词条，属昂贵操作。

**影响量级**

常驻 HUD 每帧固定开销：6 次全量效果计算 + 全部 CATUI binding 与 XML `{#...}` 表达式重算。低配机或高词条数玩家环境下明显。

**修复建议**

- 仅在**值真正变化**时才 `IsDirty`（ping、温度这类低频值直接比较后决定）；
- 效果值缓存从"按帧"改为"按状态变化"（装备/等级/技能/buff 变化时才失效），或利用已有的事件机制；
- 关闭 HUD 后（`windowManager.IsFullHUDDisabled()`）跳过全部计算。

```csharp
// 示例：ping 系列改为值变化才刷新（当前每次无条件 IsDirty=true）
case "CATUI_playerPing":
    _value = "-1";
    if (__instance.localPlayer != null)
    {
        int _ping = __instance.localPlayer.pingToServer;
        if (_ping > 0)
        {
            _value = _ping > 1000 ? ">1000" : _ping.ToString();
            // 仅当与上次不同才标记刷新，避免每帧重建 HUD
            if (_ping != _lastPing) { _lastPing = _ping; __instance.IsDirty = true; }
        }
    }
    __result = true;
    return false;
```

```csharp
// 示例：效果值缓存由"按帧"改为"按统计版本"，装备/技能变化时版本递增
private static int _statsVersion; // 订阅装备/技能/等级变化时 +1
private static int _statsCachedVersion = -1;

private static float GetMobility(EntityPlayerLocal player)
{
    if (_statsCachedVersion != _statsVersion || !_mobilityCached)
    {
        _mobility = EffectManager.GetValue(PassiveEffects.Mobility, null, 0f, player, null,
            XUiM_Player.GetPlayer().generalTags, calcEquipment: true, calcHoldingItem: true,
            calcProgression: true, calcBuffs: true, calcChallenges: true, 1, useMods: true, _useDurability: true);
        _mobilityCached = true;
        _statsCachedVersion = _statsVersion;
    }
    return _mobility;
}
```

### 3. CompassWindow 每个 binding 都重算天气

**位置**：`Source/XUiC_CompassWindowPatch.cs:11-30`（预计算块）

**问题描述**

`GetBindingValueInternalPrefix` 顶部的预计算块（`localPlayer.biomeStandingOn` → `WeatherManager.Instance.FindBiomeWeather(...)` → `stormLevel`）在**每一个 binding 查询**时都会执行，包括 vanilla 的 `compass_rotation` 等绑定。罗盘窗口每帧刷新（转动时），因此 `FindBiomeWeather` 每帧被调用 6~10 次，而它只在 `CATUI_*` 天气绑定中才真正用到。

**修复建议**：把预计算放入 CATUI binding 分支内部，并按帧缓存一次。

```csharp
// 把顶部预计算改为按帧懒缓存，只在需要天气的 binding 中取用
private static int _weatherFrame = -1;
private static BiomeDefinition.BiomeType? _biomeTypeCache;
private static WeatherManager.BiomeWeather _biomeWeatherCache;
private static int _stormLevelCache;

private static void EnsureWeatherCache(XUiC_CompassWindow __instance)
{
    int frame = Time.frameCount;
    if (frame == _weatherFrame) return;
    _weatherFrame = frame;

    var localPlayer = __instance.localPlayer;
    _biomeTypeCache = null; _biomeWeatherCache = null; _stormLevelCache = 0;
    if (localPlayer?.biomeStandingOn == null) return;

    _biomeTypeCache = localPlayer.biomeStandingOn.m_BiomeType;
    _biomeWeatherCache = WeatherManager.Instance.FindBiomeWeather(_biomeTypeCache.Value);
    _stormLevelCache = WeatherManager.currentWeather?.biomeDefinition?.currentWeatherGroup?.stormLevel ?? 0;
}
// 各 CATUI binding 内调用 EnsureWeatherCache(__instance) 后读缓存；
// vanilla binding 路径不触发任何计算。
```

---

## P2 - 物品栏模板

### 4. 传奇物品动画（item_stack 模板每格挂 UISpriteAnimation + 面板重建）

**位置**：`ZZZ_CATUI/Config/XUi_InGame/templates.xml:173`、`Source/Views/XUiV_AnimatedSprite.cs:89-93`

**问题描述**

- `<item_stack>` 模板被游戏内**所有**格子使用（背包/容器/搜刮/载具/工具栏/拖拽等），`CATUI_animatedsprite` 使每个格子都创建一个 `XUiV_AnimatedSprite` 并 `AddComponent<UISpriteAnimation>()`。
- 当"传奇品质且堆叠>5"时 `enabled`/`visible` 为 true，动画以 24fps 换帧。NGUI 中任一 widget 变脏会触发**所在 panel 每帧整体重建**（`NGUI-Decompiled/UIPanel.cs:1041-1062`），该面板内所有 drawcall 每帧重算几何。
- `CATUI_itemLegendary` 绑定在每格每次刷新时遍历全部 stat 数组（见第 5 项），进一步放大。

**影响量级**：传奇+堆叠场景较少，但一旦出现就是整面板每帧重建（48 帧 224×224 图集 UV 计算 + 网格重建）。

**修复建议**

- 降低帧率（`framerate` 16 以下）；
- 限制同时播放数量，或仅鼠标悬停 / 容器打开时播放；
- `visible`/`enabled` 条件可加频率限制（如只在刷新数据时评估，避免每帧评估表达式）；
- 若面板重建成为瓶颈，可将动画移出高频刷新面板或用静态序列帧 UV 而非换 sprite。

```xml
<!-- 建议：降低帧率并只在大地图/悬停时播放，例如改为 -->
<CATUI_animatedsprite depth="4" atlas="Animatedsprite_Legendary" pos="-5,5" x="0" y="0"
    width="${10+cell_size}" height="${10+cell_size}" sprite="frame_01" spriteprefix="frame_"
    loop="true" framerate="12"
    enabled="{# CATUI_itemLegendary and itemcount &gt; 5 and ishovered }"
    visible="{# CATUI_itemLegendary and itemcount &gt; 5 }" />
```

### 5. XUiC_ItemStackPatch 每格每刷新开销

**位置**：`Source/XUiC_ItemStackPatch.cs:80-81, 95-139, 169-215`

**问题描述**

- `GetBindingValueInternalPrefix` 开头**无条件**执行 `itemValue.ItemClass`（`GetItemClass()` 含字典/缓存逻辑），对**每一个 binding 查询**（含 vanilla binding）都白算一次；
- `CATUI_itemLegendary` 每次刷新都 `for` 遍历全部 stat 数组判断是否全 Boosted；
- `CATUI_itemStackModifications` 每次刷新都重建 StringBuilder 文本；
- 打开容器（上百格）× 每格多个绑定 × 刷新频率 → 上述操作被大量放大。

**修复建议**

- 将 `itemValue.ItemClass` 移入各 case 分支内按需取用；
- 给传奇/强化判断加"按 itemValue 版本"缓存（`ItemValue` 变化时失效）；
- 条件允许时减少该绑定所在标签的刷新频率。

```csharp
// 把无条件取值改为按需取值
[HarmonyPrefix]
[HarmonyPatch(typeof(XUiC_ItemStack), "GetBindingValueInternal")]
public static bool GetBindingValueInternalPrefix(string _bindingName, ref string _value, ref bool __result, XUiC_ItemStack __instance)
{
    switch (_bindingName)
    {
        case "CATUI_itemLegendary":
            _value = "false";
            var itemValue = __instance?.itemStack?.itemValue;
            if (itemValue == null) break;
            // 仅此处才取 ItemClass / 遍历 stats，并可用版本号缓存
            // ...
            __result = true;
            return false;
        // ... 其他 case
    }
    return true;
}
```

### 6. XUiViewPatch 字典全表操作

**位置**：`Source/XUiViewPatch.cs:9, 48-53, 57-72`

**问题描述**

- `elementScales` 静态字典对**每个**视图在 `updateData` postfix 中做 `TryGetValue`（规模大时全表查找浪费）；
- `OnClosePostfix` 遍历全表并 new 两个 List 收集待删项（每个窗口关闭时执行）；
- 视图未走 `OnClose` 时可能残留引用。

**修复建议**：只在视图确实含 `transform_scale` 时才入字典；`OnClose` 用单次遍历边收集边删除；增加视图销毁兜底清理。

```csharp
// 入表条件收紧：仅在解析到 transform_scale 且值非 1 时记录
if (_attribute == "transform_scale")
{
    float scaleValue = 1f;
    float.TryParse(_value, out scaleValue);
    scaleValue = Mathf.Clamp(scaleValue, 0.1f, 3f);
    if (Mathf.Abs(scaleValue - 1f) > 0.0001f)
        elementScales[__instance] = scaleValue;   // 缩放为 1 的不入表
    __instance.isDirty = true;
    return false;
}
```

---

## P3 - 低影响 / 设计隐患

### 7. XUiV_RoundedTexture 运行时建纹理破坏合批

**位置**：`Source/Views/XUiV_RoundedTexture.cs:78-109`

**现状**：当前 XML **未使用** `CATUI_roundedtexture`，无运行时影响。但若启用：每个实例在运行时生成一张 2x 尺寸的独立 `Texture2D`，为独立 GPU 纹理，**打破 NGUI 图集合批**（增加 draw call 与显存）。

**建议**：若启用需注意数量；或改为预烘焙/共用一张由程序生成的纹理（同尺寸圆角缓存复用），避免每实例一张。

### 8. 残留 Debug.Log

**位置**：`Source/Glow/GlowEffectPatch.cs:107,136,308`（每次建材质/InitView 都打日志）、`Source/XUiC_ItemStackPatch.cs:173`（每个空 itemValue 都打日志）

**问题**：`Debug.Log` 在 IL2CPP / Release 下仍会执行且开销不小；在 P0 修复前，GlowEffectPatch 的日志还会随每次材质重建高频触发。

**建议**：生产包移除或限频（`Debug.unityLogger.logEnabled` 开关，或仅 Debug 构建输出）。

### 9. XUiC_EquipmentStackPatch 冗余 Update 补丁

**位置**：`Source/XUiC_EquipmentStackPatch.cs:77-85`

**现状**：`[HarmonyPrefix] XUiC_EquipmentStack.Update` 中 `if (IsDirty) RefreshBindings()` 与 vanilla `XUiC_EquipmentStack.Update` 自带逻辑重复。

**建议**：删除该 prefix，减少每帧一次补丁调用。

### 10. 静态字典生命周期

**位置**：`GlowEffectPatch.cs:16-22`、`XUiViewPatch.cs:9`、`XUiC_ItemStackPatch.cs:11`、`XUiC_BuffPopoutListPatch.cs:9-12` 等

**问题**：静态 `Dictionary<XUiView/XUiC_ItemStack/EntityUINotification, ...>` 依赖 `Cleanup`/`OnClose`/`updateEntries` 清理，异常路径下可能持有已销毁引用。

**建议**：统一在视图 `Cleanup` 兜底；定期按 `Controller == null` 或 `view == null` 惰性清理。

---

## 已确认无问题

| 模块 | 结论 |
|------|------|
| UIAtlases 图集打包 | 各文件夹 PNG 由 `UIAtlasFromFolder` 打包为单张图集并缓存，加载一次、压缩、无每帧解码。3 个图集总磁盘约 1.7MB，可忽略。 |
| 图集数量 | 3 张 = 3 个材质，draw call 理想；继续合并新图进现有文件夹即可。 |
| XUiC_BuffPopoutListPatch | 10fps 限频 + UILabel 缓存 + 文本变化检测，优化到位。 |
| Party / Target / Skill 等窗口级绑定 | 只在窗口刷新时执行，非每帧。 |
| Video ChromaKey 补丁 | `ParseInitialAttributeValuePrefix` 正确限定 `XUiV_Video` 类型，无全局污染。 |
| XUiUpdaterPatch | 每帧仅两次输入检测，按键时才查窗口/实体，可接受。 |

---

## 优化优先级路线图（按投入产出比）

1. **P0-1 glow 补丁**：修复/禁用（一次改动，全局帧率收益最大，强烈建议最先做）
2. **P1-2 HUD 绑定**：IsDirty 改为值变化触发 + 效果值缓存改状态化
3. **P1-3 罗盘**：天气预计算按帧缓存，移入 CATUI binding 分支
4. **P2-4 传奇动画**：降帧率 + 播放条件收紧
5. **P2-5 ItemStack 绑定**：按需取值 + 版本缓存
6. **P2-6 / P3**：字典收窄、删冗余补丁、清理日志与静态字典
7. 完成 1~6 后，用 Profiler（Deep Profile + 每帧 `XUi/NGUI Update` 采样）复测，对比修复前后帧耗时。
